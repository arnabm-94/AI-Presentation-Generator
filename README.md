# AI-Presentation-Generator
This script automatically generates a PowerPoint presentation using Python.  It creates a new presentation file, adds multiple slides with predefined layouts, and fills each slide with structured content related to the info filled in the code. The information can be dynamic. 
